class Solution(object):
    def maxProfit(self, prices):
        #You are given an array prices where prices[i] is the price of a given stock on the ith day.
        # Find the maximum profit you can achieve. You may complete at most two transactions.
        # Note: You may not engage in multiple transactions simultaneously 
        # (i.e., you must sell the stock before you buy again)
        #constraints: 1 <= prices.length <= 105 and 0 <= prices[i] <= 105
        """
        :type prices: List[int]
        :rtype: int
        """
        #solution:
        #1. find the max profit for the first transaction
        #2. find the max profit for the second transaction
        #3. find the max profit for the third transaction
        #4. find the max profit for the fourth transaction
        #5. find the max profit for the fifth transaction
        #6. find the max profit for the sixth transaction
        #7. find the max



        